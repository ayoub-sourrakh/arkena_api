set -euo pipefail

echo "==> bundler-audit"
bundle exec bundler-audit check

echo "==> RuboCop"
bundle exec rubocop

echo "==> Brakeman (quiet)"
bundle exec brakeman -q

echo "==> RSpec"
bundle exec rspec --format progress